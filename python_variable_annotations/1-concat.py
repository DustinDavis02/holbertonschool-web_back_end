#!/usr/bin/env python3
"""Function that concats two strings and returns a concatenated string"""


def concat(str1: str, str2: str) -> str:
    """Concatenates two strings and returns a concatenated string"""
    return str1 + str2